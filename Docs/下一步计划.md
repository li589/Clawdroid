# Clawdroid 下一步计划

> 状态基准：以**本文更新日期**与工作区代码为准（不再钉死单次 commit hash）  
> 更新日期：2026-07-27  
> 配套文档：[architecture.md](architecture.md)、[release-notes.md](release-notes.md)、[AGENTS.md](../AGENTS.md)

## 1. 现状摘要

### 已稳定

| 能力 | 证据 |
|------|------|
| 双主体底座（App + Magisk Runtime） | 开源基线 `v0.1.0` + IPC 协议 |
| Runtime 闭环（握手 → Ready → 截图/注入/文件/shell/事件/`task_*`） | 兼容矩阵首行设备 |
| Compose 三页壳 + 设置分类导航 | Hub → Category |
| Agent 工具环（可配置步数 / API 预算 / LoopDetector） | `ai/` + 设置「Agent 与工具」 |
| SoftWarn 真正跳过执行 | `AgentToolLoopController` + `ToolLoopDetectorTest` |
| `list_dir_limited` / `file_list` | Runtime + App 对齐（需更新 Magisk 模块） |
| IPC 分动作读超时 + shell/截图服务端 deadline | App `ClawRuntimeIpcClient` + Runtime `server.go`；需同轮 Magisk ZIP |
| App↔Runtime 能力对齐 banner | `RuntimeCompatSnapshot` + 概览/诊断 |
| 聊天当前轮附图（OpenAI / Anthropic） | `ChatImageEncoder` + `ModelApiClient` |
| `task_wait` + 取消/detach 协同 | App 工具（非 IPC）+ `RuntimeTaskPoller` |
| Termux 坏容器一键清理 | 设置 → Termux 与 Shell |

### 已落地但验收未闭环

| 能力 | 缺口 |
|------|------|
| Assist MCP | [assist-mcp.md](assist-mcp.md) 清单多数未勾选 |
| Termux 深度接入 | 需真机 `allow-external-apps` + `RUN_COMMAND`；坏容器清理需烟测 |
| 域工具冒烟 | 同设备系统性勾选不足 |
| Xposed 深回归 | 目标 App 证据不足 |
| 兼容矩阵 | 仍仅一台完整证据 |
| 可靠性六项真机烟测 | 见 [真机安装验收清单.md](真机安装验收清单.md) §8 |

## 2. 原则

1. **先验收、再加深、后扩张**
2. 改协议必须同步 Go + Kotlin + `protocol.md` + catalog check
3. 新功能落正确包边界（见 [AGENTS.md](../AGENTS.md)），避免回流上帝类
4. Xposed 保持浅层感知，不做深度业务自动化默开

## 3. 建议执行顺序

### P0 — 验收收口

1. Assist MCP 联调清单勾选
2. 工具冒烟：`file_list` / `file_*` / `sandbox_shell` / `termux_exec`（有 Termux 时）/ `shizuku_*`
3. 重装 Magisk ZIP 验证 `list_dir_limited`（问「列出 /sdcard」应走 `file_list`）**以及本轮 IPC deadline / 对齐 banner**
4. 可靠性六项真机烟测（§8）结果回填兼容矩阵或本文件进度日志

### P1 — 稳定性与 CI

1. 第二台真机写入兼容矩阵
2. CI 强制 `:app:testDebugUnitTest`
3. 继续削减 `OverviewController` 技术债（可选）

### P2 — 任务化 Agent 闭环（部分已落地）

1. ~~`task_wait` + 取消时 `task_cancel` + detach 软状态~~（代码已落地，待真机勾选）
2. 聊天任务台与 Runtime `task_*` / 事件更深同步（WaitingSignal / 人工放行 IPC **不做**，协议未齐）
3. 多 Agent 并行与任务卡 UI 体验打磨

### P3 — Runtime / 适配治理

见 [基础方案设计.md](基础方案设计.md) 阶段叙述；不阻塞 P0/P1。

## 4. 进度日志

| 日期 | 摘要 |
|------|------|
| 2026-07-27 | **可靠性六项**：Compat banner、IPC 分动作超时 + Runtime shell/截图清 deadline、SoftWarn 跳过执行、Termux 坏容器清理 UX、聊天当前轮多模态附图、`task_wait`/取消/detach；Magisk ZIP 已重建；文档 `architecture` / `protocol` / `release-notes` / 真机清单 §8 同步 |
| 2026-07-26 | 架构 B：文档权威化（architecture / AGENTS / 模块 README）；`list_dir_limited` 写入 protocol；代码侧 data 包 / Chat 拆分 / ToolHandler / Settings 分文件 / CompositionRoot（见架构重构计划） |
| 2026-07-18 | 文档与 Agent/工具/MCP 扩张面首次对齐（历史） |
