# Clawdroid 目录说明

## 目录目标

本说明用于解释当前仓库的顶层目录划分，明确哪些目录属于 Clawdroid 主工程，哪些目录属于正式文档，哪些目录只是第三方参考资料，从而避免继续把 Brain Host、Runtime Executor 与外部调研源码混在一起理解。

## 顶层目录

### `ClawApp/`

用于存放 Android 客户端相关内容，代表面向用户安装的 APK 工程。

当前应将其理解为 `Clawdroid App` 的源码目录，也就是整个系统的 Brain Host。

当前包含：

- `settings.gradle.kts`
  - `ClawApp` 独立 Gradle 工程入口
- `build.gradle.kts`
  - `ClawApp` Gradle 根插件版本声明
- `gradle.properties`
  - `ClawApp` Android/Gradle 构建属性
- `local.properties`
  - Android SDK 本地路径
  - 可选包含 `clawdroid.runtime.sharedSecret`
  - 属于本地开发文件，不应作为主仓共享配置
- `app/`
  - Android App 主模块
  - 包结构（`com.clawdroid.app`）大致为：
    - `ui/` — Compose 壳（Overview / Chat / Settings）与 ViewModel
    - `ai/` / `chat/` / `orchestrator/` — Agent 编排、提示规划、直连命令
    - `model/` — 多供应商 Model API 客户端
    - `tools/` / `skills/` — 工具目录、调度、域服务与技能
    - `mcp/` — 手机 MCP Server 与 Assist 客户端
    - `runtime/` / `ipc/` — Runtime 高层客户端与 UDS 帧/鉴权
    - `env/` / `shizuku/` — 环境探测、权限、Shizuku
    - `service/` — 无障碍与通知监听
    - `focus/` / `xposed/` — 焦点/view 存储与 LSPosed 适配器
    - `fault/` / `automation/` — 故障隔离与自动化状态
- `xposed-stubs/`
  - 本地编译桩模块
  - 用于在不直接依赖外部 Xposed API 仓库的情况下完成编译期接口引用

### `ClawRuntime/`

用于存放底层执行器与 Magisk 模块相关内容，代表高权限运行时工程。

当前物理目录名已统一为 `ClawRuntime/`。

当前包含：

- `runtime/`
  - Go 守护进程工程
  - 当前承载 UDS 服务端、协议处理、鉴权、审计和受限执行能力
- `magisk/`
  - Magisk 模块打包目录
  - 包含 `module.prop`、`service.sh`、`sepolicy.rule`、配置文件与 `webroot/` 状态快照产物
- `scripts/`
  - 打包、编译、分发辅助脚本目录
  - 包含 `build-runtime.ps1`、`build-magisk.ps1`、`sync-shared-secret.ps1`

### `Docs/`

用于存放项目正式文档，不与代码目录混放。

当前包含：

- `下一步计划.md` — 当前优先级、完成标准与进度日志
- `基础方案设计.md` — 架构与分阶段路线
- `protocol.md` — Runtime IPC 协议
- `threat-model.md` — 威胁模型与信任边界
- `assist-mcp.md` — 协助 MCP 与工具地基契约
- `xposed-adapters.md` — LSPosed 适配器 / focus·view 契约
- `目录说明.md` — 本文件
- `真机安装验收清单.md`
- `发布前清单.md`
- `compatibility-matrix.md`
- `release-notes.md`

其中 `基础方案设计.md.bak` 仅作为历史备份文件保留，不作为当前正式文档入口。

### `GitSource/`

用于存放外部参考源码与调研资料。

该目录的定位必须明确为：

- 不是当前主工程实现目录
- 不是主仓构建前提
- 不是发行交付物
- 不受主仓根级 `MIT` 许可证重新授权

当前保留该目录的目的主要是：

- 对比 Root / Magisk / Zygisk / LSPosed 相关上游实现
- 为后续兼容性调研和风险评估提供参考

第三方许可证边界说明见仓库根 [THIRD_PARTY_NOTICES.md](file:///d:/temp_desktop/Proj/Clawdroid/THIRD_PARTY_NOTICES.md)。

### 其他顶层内容

- `README.md`
  - 对外项目说明与开发者入口
- `LICENSE`
  - 主仓许可证
- `CONTRIBUTING.md`
  - 贡献规范
- `CODE_OF_CONDUCT.md`
  - 社区行为准则
- `SECURITY.md`
  - 安全问题提交说明
- `.github/`
  - 持续集成与协作模板

## 当前推荐理解方式

从项目视图上看，仓库应理解为：

1. `ClawApp` 目录承载的是 `Clawdroid App`，它是用户层、AI 层和工具编排层的总入口
2. `ClawRuntime` 目录是 Root 执行侧和 Magisk 打包侧宿主
3. `Docs` 是架构、协议、安全和目录规范的正式文档区
4. `GitSource` 是受边界约束的参考资料区，不参与主仓功能归属判断

补充说明：

1. 共享密钥推荐由仓库根 `local.properties` 统一保存，通过构建脚本同步到 App 与 Magisk 模块；该文件属于本地私有配置。
2. `ClawRuntime/magisk/webroot` 当前承载的是本地诊断快照，不是长期开放的远程控制台。

## App 内部分层建议

在 `Clawdroid App` 内部，推荐按「编排 → 工具 → 执行通道」理解，而不是只认三个包名：

1. **编排层**（`orchestrator` / `ai` / `chat` / `skills`）
   - 把聊天文本、模型工具调用或技能解析成明确意图与参数
2. **工具层**（`tools` / `mcp`）
   - 统一目录、权限门、并发策略与执行结果；MCP 是同一工具面的外部暴露
3. **执行通道**（`runtime` / `ipc` / `service` / `shizuku` / `xposed`+`focus`）
   - Root Runtime IPC、无障碍/通知、Shizuku、LSPosed 感知分别作为可降级通道

这样划分后，项目表面结构会更清晰，也更符合“两个主体程序 + 一组正式文档 + 一组第三方参考资料”的组织目标。

当前执行优先级见 [下一步计划.md](下一步计划.md)。
